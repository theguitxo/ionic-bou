import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.guitxo.boudefoc',
  appName: 'El Bou de Foc del Vendrell',
  webDir: 'www'
};

export default config;
